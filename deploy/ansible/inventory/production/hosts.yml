---
all:
  children:
    afripay:
      children:

        # Serveurs d'application 
        api:
          hosts:
            afripay-prod-01:
              ansible_host: "{{ lookup('env', 'PROD_HOST') | default('193.168.145.162') }}"
              ansible_port: 22
              ansible_user: afripay

        # Serveurs de supervision 
        monitoring:
          hosts:
            afripay-prod-01:  # même serveur, rôle monitoring

  vars:
    ansible_python_interpreter: /usr/bin/python3