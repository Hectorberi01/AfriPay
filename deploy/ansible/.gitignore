# Le vault staging chiffré est versionné (AES256 — sûr à commiter)
# Le vault production reste hors dépôt (chiffrer avant de commiter)
inventory/production/group_vars/all/vault.yml
*.retry

# Clés SSH
*.pem
*.key
id_rsa*

# Cache Ansible
.ansible/
/tmp/ansible_facts/

# Logs
*.log